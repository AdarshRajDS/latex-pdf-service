[project]
name = "latex-pdf-service"
version = "0.1.0"
description = "Production-grade LaTeX to PDF API"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings"
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "httpx"
]
